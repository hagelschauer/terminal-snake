# Terminal Snake
Just some good old snake for the terminal.

This is really just a tiny little fun project, so don't expect too much.

# Build
As it is written in Rust you can build it using [cargo](https://rust-lang.org/tools/install/)
- Run `cargo build --release`.
- The binary is then located in `target/release/`.
